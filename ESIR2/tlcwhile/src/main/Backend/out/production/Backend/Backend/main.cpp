#include "while_runtime.h"
#include <stack>
#include <vector>
#include <iostream>

typedef Tree TreeNode;

TreeNode* sub(TreeNode* Op1, TreeNode* Op2) {
    TreeNode* R1 = Op1;
    TreeNode* R2 = Op2;
    TreeNode* Result = nullptr;
    TreeNode* R3 = Result;
    R3 = Op1;
if (break_for_L2 == nullptr) {
    Result = nullptr;
    TreeNode* R4 = Result;
    R4 = tl(Result);
    Op2 = tl(Op2);
   goto L1;
}
    return Result;
}
int main (){
    return 0;
}
